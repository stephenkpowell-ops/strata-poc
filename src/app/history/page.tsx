'use client';

/**
 * src/app/history/page.tsx
 *
 * History screen — placeholder for a future step.
 *
 * This route exists so the History tab in NavBar does not 404.
 */

export default function HistoryPage() {
  return (
    <div className="flex flex-col flex-1 items-center justify-center min-h-screen bg-zinc-950 gap-3">
      <span className="text-zinc-400 text-base font-semibold">History</span>
      <span className="text-zinc-600 text-sm">Coming soon</span>
    </div>
  );
}
