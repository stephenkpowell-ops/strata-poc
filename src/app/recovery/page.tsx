'use client';

/**
 * src/app/recovery/page.tsx
 *
 * Recovery screen — placeholder for step 22 (Breathing Reset protocol).
 *
 * This route exists so the Recovery tab in NavBar does not 404.
 * The full protocol picker and breathing session will be built in step 22.
 */

export default function RecoveryPage() {
  return (
    <div className="flex flex-col flex-1 items-center justify-center min-h-screen bg-zinc-950 gap-3">
      <span className="text-zinc-400 text-base font-semibold">Recovery</span>
      <span className="text-zinc-600 text-sm">Coming in step 22</span>
    </div>
  );
}
